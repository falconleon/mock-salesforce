# Thrum data directory (all data lives on a-sync branch via worktree).
# Whitelist the project_state.md so coordinators can track it across sessions.
.thrum/*
!.thrum/context/
.thrum/context/*
!.thrum/context/project_state.md
.thrum.*.json
scripts/thrum-startup.sh
scripts/thrum-check-inbox.sh

# Claude Code workspace
.claude/

# Compiled binary
salesforce-mock

# Air hot reload temp
tmp/

.playwright-cli
.env

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads
.intent
